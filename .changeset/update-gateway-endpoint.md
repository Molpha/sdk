---
"@molpha-oracle/sdk": patch
---

update the default gateway endpoint to `https://gateway.molpha.io`

`DEFAULT_GATEWAY_ENDPOINT` now points at the production Molpha gateway instead of the
previous dev IP address.
